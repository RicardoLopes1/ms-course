spring:
  application:
    name: hr-user
  cloud:
    config:
      uri: http://localhost:8888
  profiles:
    active: test

# actuator config
management:
  endpoints:
    web:
      exposure:
        include:
          - "*"
